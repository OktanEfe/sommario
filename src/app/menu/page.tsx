import MenuClient from './MenuClient';

export const metadata = {
  title: 'Menü',
  description: 'Sommario\'nun özel kahve ve içecek menüsünü keşfedin.',
}

export default function MenuPage() {
  return <MenuClient />;
}